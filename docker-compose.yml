version: "3.8"

services:
  satoshi-scraper:
    build: .
    container_name: satoshi-scraper
    restart: unless-stopped
    ports:
      - "9119:9119"
    environment:
      - PORT=9119
      - NODE_ENV=production
      - MEMPOOL_SPACE_WS_URL=${MEMPOOL_SPACE_WS_URL:-wss://mempool.space/api/v1/ws}
      - MEMPOOL_SPACE_RECONNECT_MS=${MEMPOOL_SPACE_RECONNECT_MS:-1000}
      - MEMPOOL_KNOTS_HTTP_BASE=${MEMPOOL_KNOTS_HTTP_BASE:-https://knotapi.zatobox.io}
      - MEMPOOL_KNOTS_HTTP_POLL_INTERVAL_MS=${MEMPOOL_KNOTS_HTTP_POLL_INTERVAL_MS:-1000}
      - HTTPS_REDIRECT_HOST=${HTTPS_REDIRECT_HOST:-api.zatobox.io}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:-https://zatobox.io,https://www.zatobox.io}
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9119/health"]
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3
    volumes:
      - ./cache:/app/cache
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
